"""SQLite FTS5 index over vault markdown notes.

Implemented in a later step. This module will own the schema, the initial
build from disk, incremental updates driven by change events, and the
ranked query.
"""
